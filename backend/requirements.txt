# Runtime dependencies for the Smart Home Home Assistant backend.
# Keep this file to direct project dependencies; pip resolves transitive deps.

fastapi>=0.115,<1.0
pydantic>=2.7,<3.0
python-dotenv>=1.0,<2.0
requests>=2.32,<3.0
uvicorn[standard]>=0.30,<1.0

# Test dependency used by fastapi.testclient.TestClient.
httpx>=0.27,<1.0
